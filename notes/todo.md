2026-04-06

- Decide whether to cap the number of external review iterations or require explicit approval only.
- Add a lightweight status helper if detached-run monitoring becomes cumbersome.
- Add a tiny local render-check helper that inspects SVG line breaks or bounding boxes before export; this run spent time catching header and label wrapping issues after rendering.
